import React from "react";
import Card from "../../shared/components/UIElements/Card";
import "./UserPlaces.css";

const UserPlaces = (props) => {
  if (props.length === 0) {
    <Card>
      <h1 className="place-">Sorry no places found</h1>
    </Card>;

    re;
  }
};

export default UserPlace;
